import {Link} from "react-router-dom";
import {
  Callout,
  ConceptSection,
  GrammarBox,
  Math,
  MathBlock,
  PipelineDiagram,
  ReferenceList,
  RuleAnatomy,
  RuleCardStrip,
  SectionHeading,
  SummaryBox,
  TryItBox,
} from "@/features/docs/lectures/blocks/LectureBlocks.tsx";
import {MiniWorkspace} from "@/features/docs/workspace/MiniWorkspace.tsx";
import {PredictThenVerify} from "@/features/docs/workspace/PredictThenVerify.tsx";
import {AstBuilder} from "@/features/docs/workspace/AstBuilder.tsx";
import {ReductionStrategyExplorer} from "@/features/docs/workspace/ReductionStrategyExplorer.tsx";

Three questions carry this whole lecture: what does a term look like, does it make sense,
and what does it do when it runs? That's syntax, typing, and semantics — the same three
questions every language in this app (and this course) answers, just with more machinery
added lecture by lecture. Take it slowly: every idea below gets a plain-English analogy
before the notation, and something you can actually run before you're asked to trust it.

<SectionHeading id="why-any-of-this" index="0" title="Why any of this?" blurb="A minute of context before the formalism starts." />

<ConceptSection id="what-is-stlc" title='What does "STLC" even mean?'>

STLC stands for **Simply Typed Lambda Calculus** — three words, three ideas, read right to left:

- **Lambda calculus** is the tiny language of functions this whole course is built on.
  `λ` is just the Greek letter Alonzo Church picked, in the 1930s, to mean "here comes a function."
- **Typed** means every piece of the program has to have one specific, checked-in-advance
  type — never a guess, never "we'll find out at runtime."
- **Simply** means the smallest useful version of "typed": no generics, no advanced tricks.
  Just enough to be real. Everything fancier (lectures 5–11) gets added on top, one idea at a time.

</ConceptSection>

<ConceptSection id="types-and-grammars" title="Types and grammars, in plain terms">

- A **type** is a promise about a value: "this is a `Bool`" means it's always `true` or
  `false`, never accidentally a number. **Type-checking** is just mechanically verifying
  every promise in a program is kept — before any of it runs.
- A **grammar** is a rulebook for what counts as a valid sentence — same idea as "the cat
  sat" being fine in English but "cat the sat" not. This lecture's grammar does that for
  terms: it decides which strings of symbols are even worth asking a type about.

</ConceptSection>

<ConceptSection id="flavors-of-typing" title="The type-system vocabulary you've already been using">

Before going further, it's worth naming three independent choices every language makes —
you've felt all of them, just maybe not by these names:

- **Static vs. dynamic.** Static means types are checked *before* anything runs — like
  spell-checking an essay before you hand it in. Dynamic means a value's type is only
  discovered *while running* — like a teacher only catching a typo the moment they read
  your essay aloud. TT, like Java, Rust, or TypeScript, is static.
- **Strong vs. weak.** Strong means the checker never quietly converts one type into
  another — a strict bouncer who won't wave through the wrong ID no matter how close it
  looks. Weak means some conversions happen silently (JavaScript turning `"5" - 1` into
  `4`). TT is strong: nothing gets silently reinterpreted.
- **Implicit vs. explicit.** Explicit means you write the type yourself, like
  `λx:Bool. x` does below. Implicit means the checker guesses it for you, the way
  TypeScript's `const x = 5` never makes you write `: number`. This lecture is fully
  explicit; lecture 8 (type inference) is what makes TT implicit too.

TT today is **static, strong, and explicit** — the strictest, most spelled-out corner of
that whole space, which is exactly why it's the clearest place to start.

</ConceptSection>

<ConceptSection id="where-this-comes-from" title="Where this comes from">

In 1901, Bertrand Russell found a contradiction sitting inside the foundations of
mathematics itself — a single sentence that was both true and false. A few decades
later, mathematicians David Hilbert and Wilhelm Ackermann sharpened the resulting unease
into one precise question, the *Entscheidungsproblem*: is there a single mechanical
procedure that can decide, for any mathematical statement, whether it's true? Kurt Gödel
had already shown, with his incompleteness theorems, that any system powerful enough to
be useful can't prove every truth about itself — a strong hint the answer might be no.

Three people answered the *Entscheidungsproblem* independently, within a few years of
each other. Alan Turing described it with an imagined machine reading and writing symbols
on an infinite tape. Gödel and Stephen Kleene described it with recursive functions built
from a handful of primitive operations. Alonzo Church, who happened to be Turing's own
doctoral advisor, answered with a small notation for functions: the λ-calculus. All three
turned out to define the *exact same* boundary of what's computable — and all three
proved the answer to Hilbert's question was no: some things genuinely can't be decided
mechanically. Church's version is the one TT builds on.

That's not just trivia. The question TT keeps asking, lecture after lecture — "does this
term make sense?" — is the same one TypeScript, Rust, or Java answers before your code
ever runs. And the undecidability result above isn't just history, either — later in this
lecture you'll meet a small, concrete term that loops forever no matter what, a tiny taste
of the exact same limit Gödel, Turing, and Church proved in general.

</ConceptSection>

<ConceptSection id="where-youll-see-this-again" title="Where you'll see this again">

Every statically-typed language you've used already runs a version of what TT
teaches — usually hidden behind a compiler. A handful of ideas worth naming now,
since you've likely met them already without the name:

- **Type inference** — TypeScript, Rust, and Kotlin guessing a type you didn't
  write — is the Γ ⊢ t : T judgement, solved automatically instead of checked
  against an annotation (lecture 8).
- **Generics** — explicit type parameters like `T` in Java, TypeScript, or Rust —
  are explicit polymorphism (lecture 9) with friendlier syntax.
- **Option/Result** types — the reason modern languages don't crash on null — are
  sum types (lecture 5) doing real work.

None of it needs memorizing here — just recognizing later, once you've seen the mechanism underneath it.

</ConceptSection>

<PipelineDiagram
  steps={[
    {label: "Source text", detail: "what you type"},
    {label: "AST", detail: "parsed shape"},
    {label: "Γ ⊢ t : T", detail: "does it typecheck?"},
    {label: "Value", detail: "what it runs to"},
  ]}
/>

<SectionHeading id="syntax" index="1" title="Syntax" blurb="What does a term look like, and what's its shape underneath?" />

<Callout title="The app, alongside this page" screenOnly>

Everything here pairs with the real thing — open **Editor** in the top nav to
follow along. It's one screen: a text editor on the left, and panels on the right
(**AST**, **Proof Tree**, **Evaluation**) that update live as you type. This
lecture introduces each panel one at a time, right where it becomes relevant.

</Callout>

<ConceptSection id="warm-up" title="A warm-up: computing without any functions yet">

Before functions enter the picture at all, the app's grammar already gives you a tiny
calculator that also understands yes/no questions — `true`, `false`, numbers, `if ...
then ... else ...`, and `+ - * /`. That's already enough to write real, checkable
programs:

```
if true then 1 else 2;
```

Try editing the box below — this is the same live checker the rest of the lecture uses,
just with nothing but constants so far, no `λ` required:

</ConceptSection>

<MiniWorkspace
  initialTerm={"if true then 1 else 2;"}
  label="Try it — no functions needed"
  hint='Now try `true + 1;`. It parses just fine — grammatically, "true plus one" is a perfectly valid term shape — but the checker rejects it. That gap, between "parses" and "makes sense", is exactly why typing rules exist, coming up in the next section.'
/>

<ConceptSection id="terms" title="Terms">

A **term** is just one piece of program text — think of it like a single LEGO
brick, or one ingredient in a recipe. On its own it's already a well-formed piece;
you build a whole program by snapping terms together. `5`, `true`, and `x` are all
terms by themselves; so is a whole function like `λx:Bool. x`.

Every term the app understands is built from just four shapes:

- `x` — a **variable**, standing for whatever it's bound to.
- `λx:T. t` — an **abstraction**: a function with parameter `x` of type `T` and body `t`.
- `t u` — an **application**: term `t` called with argument `u`. No symbol for
  "call" — you just write the two terms next to each other, with a space between.
  Chain three terms and it groups from the left, so `t u v` means `(t u) v` — call
  `t` with `u` first, then call whatever that gives back with `v`.
- `true`, `false`, numerals, and `if ... then ... else` — the constants from the
  warm-up above, formally part of the same grammar as everything else.

That's it — everything in later lectures (tuples, lists, recursive types,
polymorphism...) is more shapes added on top of these four. The full grammar
always lives on the [Grammar & Symbols](/docs/grammar) page; below is just the
slice this lecture uses.

</ConceptSection>

<ConceptSection id="the-arrow" title="The arrow: how a function's type is written">

`λx:T1. t2`'s type is written `T1 -> T2` — "a function that takes a `T1` and gives
back a `T2`". Think of it like a vending machine slot: put in exactly a `T1` (the
right coin), and you're guaranteed a `T2` back, no matter what happens inside.

`->` is **right-associative**, so `T1 -> T2 -> T3` means `T1 -> (T2 -> T3)` — "a
function that takes a `T1` and hands back *another function*, from `T2` to `T3`".
That's exactly what a multi-parameter function looks like once it's written as
nested single-parameter abstractions:

- `λ x : Bool . λ y : Bool . x` has type `Bool -> Bool -> Bool`, i.e.
  `Bool -> (Bool -> Bool)` — give it one `Bool`, get back a function still waiting
  for the second one.

Each arrow peels off exactly one argument — the same trick every curried function
in TS, Haskell, or Rust is built from.

</ConceptSection>

<GrammarBox grammar={`term ::= ID                                     (* variable *)
       | 'λ' ID ':' type '.' term               (* abstraction *)
       | term term                              (* application *)
       | term ('+'|'-'|'*'|'/') term             (* arithmetic *)
       | 'if' term 'then' term 'else' term
       | 'true' | 'false' | natural-number
       | '(' term ')'

type ::= ID | 'Bool' | 'Nat' | type '->' type          (* right-associative *)`} />

<ConceptSection id="text-to-tree" title="From text to tree">

What you type is **concrete syntax** — characters, spacing, parentheses. What the
parser hands to the typechecker and evaluator is **abstract syntax**: a tree with
the formatting stripped away, keeping only the shape. `(λ x : Bool . x) true` becomes:

- an **application** node, whose two children are —
- an **abstraction** node (parameter `x`, type `Bool`, body `x`), and
- a **literal** node (`true`).

Every rule from here on — typing rules, evaluation rules — is defined by matching
on this tree shape, not on the source text. The app's **AST** panel shows you this
tree live for whatever you type; try building the same tree by hand below, using
only the pieces introduced so far.

</ConceptSection>

{/* typeVar is included only because param type is a connectable node, not an inline field. */}

<AstBuilder
  allowedTypes={["abstraction", "application", "variable", "literal", "typeVar"]}
  instructions="Construct (λ x : Bool . x) true: an Application, whose two children are an Abstraction (param x : Bool, body a Variable x) and a Bool Literal (true). The palette below only offers what this lecture has covered — no jumping ahead."
/>

<ConceptSection id="free-bound" title="Names: free vs. bound">

A variable occurrence is **bound** if it's inside the body of a `λ` that
introduces it, and **free** otherwise — the same distinction as using someone's
name in a sentence without ever introducing who they are (free) versus saying
"let `x` be..." first and only then using `x` (bound):

- `λx:Bool. x` — `x` is bound (by the outer `λx`).
- `λx:Bool. y` — `y` is free; nothing in the term binds it, so its meaning must
  come from outside (the context, Γ — covered in the next section).
- `λx:Bool. λy:Bool. x` — `x` is bound by the outer `λ`, `y` by the inner one.
  Both are bound; nothing is free.

A term with *no* free variables at all — like `λx:Bool. x` above — is called a
**combinator**: it's fully self-contained, needing nothing from outside itself to
make sense. You'll meet more combinators, including the famous Y combinator, in
the Church encodings lecture.

</ConceptSection>

<ConceptSection id="alpha-equivalence" title="α-equivalence: names don't matter, bindings do">

A bound variable is just a placeholder for "whatever gets passed in" — renaming
it consistently throughout a term changes nothing about what it means:

- `λx:Bool. x` and `λy:Bool. y` — the *same* function, just spelled with a
  different parameter name. This is **α-equivalence**.
- `λx:Bool. y` and `λx:Bool. z` — genuinely *different* terms, since `y` and `z`
  are free and refer to two different things outside the term.

The rule of thumb: only which occurrences are bound *together* is meaningful — the label itself never is.

</ConceptSection>

<SectionHeading id="typing" index="2" title="Typing" blurb="Given the shape above, does this particular term make sense?" />

<ConceptSection id="why-bother" title="Why bother?">

Remember `true + 1` from the warm-up? The grammar above doesn't stop you from
writing it — applying `+` to a boolean is syntactically fine, it just doesn't
mean anything. Left alone, a term like that gets **stuck** during evaluation:
not a value, but no rule applies to it either.

Typing catches this *before* anything runs, by answering one question for every
subterm: given what's in scope, does this make sense, and what does it produce?
That question has a name — the **typing judgement**:

<MathBlock>{"\\Gamma \\vdash t : T"}</MathBlock>

- Read it as "under context Γ, term `t` has type `T`".
- `Γ` (capital gamma) is the **context** — think of it as the list of parameters
  currently in scope, each paired with its type, the same way a function
  signature tells you what's available inside its body.
- A closed term (no free variables — a combinator, from the Syntax section) can
  be checked against the empty context; a term with free variables needs Γ to
  say what they are.

</ConceptSection>

<ConceptSection id="how-to-read-a-rule" title="How to read a typing rule">

A rule is a recipe: a short list of things that must already be true — the
**premises**, above the line — and what follows if they are — the
**conclusion**, below it. Just like a real recipe: "if you have these ingredients
ready (premises), you can make this dish (conclusion)." No premises at all just
means "always true" — a recipe that needs nothing.

</ConceptSection>

<RuleAnatomy />

Five such rules cover everything this lecture's grammar can express — one per term shape, plus the constants:

<RuleCardStrip ruleIds={["T-Var", "T-Abs", "T-App", "T-If"]} />

<Callout title="Examples, and staying in plain STLC" screenOnly>

The **Examples** dropdown in the top bar has pre-written terms if you'd rather
load one than type it. Next to it, **Type System Extensions** lets you turn on
later lectures' features (polymorphism, recursive types, dependent types...) —
leave everything off for now. This lecture only needs plain STLC.

</Callout>

<TryItBox
  steps={[
    <>Open the <strong>Editor</strong> and clear it, then type <code>(λ x : Bool . x) true;</code>.</>,
    <>Look at the <strong>AST</strong> panel — the same application/abstraction/literal shape you just built by hand.</>,
    <>Switch to the <strong>Proof Tree</strong> panel — it's the Γ ⊢ t : T judgement built live, one rule per node, matching the T-App/T-Abs/T-Var/T-Const cards above.</>,
    <>Break it on purpose: change <code>true</code> to <code>5</code>. T-App's premise that the argument's type matches the parameter's type now fails, and the error shows exactly where.</>,
  ]}
/>

Same idea, without leaving this page — edit the term and check its type:

<MiniWorkspace
  initialTerm={"(λ x : Bool . x) true;"}
  hint="Try replacing `true` with a number, or dropping the argument entirely, and see which rule's premise breaks."
/>

<PredictThenVerify
  term={"λ f : Bool -> Bool . λ x : Bool . f x;"}
  prompt="Checkpoint: this function takes another function and a Bool. Work out its type before revealing — T-Abs applied twice, from the inside out."
/>

<SectionHeading id="semantics" index="3" title="Semantics" blurb="The term type-checks — now what does it actually do when it runs?" />

<ConceptSection id="math-functions" title="The same idea as ordinary functions">

Ordinary function notation and λ-notation say the same thing two ways:

- Math: `f(x) = x + 1`, then `f(3) = 4`.
- The app: `f = λ x : Nat . x + 1 : Nat -> Nat`, then `f 3` reduces to `4`.

"Calling `f` with `3`" and "replacing `x` with `3` throughout the body" are the
same operation. That replacement has a name — **β-reduction** — and, unlike the
informal math version, it's defined precisely enough for a computer to carry out.

</ConceptSection>

<ConceptSection id="substitution" title="Substitution, precisely">

**Substitution** is a fancy name for a simple idea: find-and-replace, like in a
text editor — swap every occurrence of one thing for another. Written `[x ↦ v]t`,
it means "`t`, with every free occurrence of `x` replaced by `v`". It's defined
by recursing over `t`'s shape — the same four shapes from the Syntax section:

- `[x ↦ v] x = v`
- `[x ↦ v] y = y`, if `y ≠ x` — leaves other variables alone.
- `[x ↦ v] (λy:T. t) = λy:T. [x ↦ v] t`, if `y ≠ x` — recurses into the body, as
  long as the inner binder doesn't shadow `x`.
- `[x ↦ v] (t₁ t₂) = ([x ↦ v] t₁) ([x ↦ v] t₂)` — recurses into both sides of an application.

β-reduction is exactly one application of this: `(λx:T. t) v → [x ↦ v] t`. For example:

<MathBlock>{"(\\lambda\\, x : \\mathsf{Nat}\\, .\\; x + 1)\\; 3 \\rightarrow 3 + 1 \\rightarrow 4"}</MathBlock>

</ConceptSection>

<ConceptSection id="eta-reduction" title="A quieter cousin: η">

There's one more equivalence worth knowing, even though this app's evaluator
doesn't need to perform it for you: **η-reduction**. A function that does
nothing but immediately hand its argument to another function — `λx:T. f x`,
where `x` doesn't appear free in `f` — behaves *exactly* like `f` on its own,
for every possible input. It's the functional-programming version of "why write
a one-line wrapper that just forwards its argument, when you could call the
thing directly?" Together, α (renaming), β (running a function), and η
(dropping a pointless wrapper) are the three core equivalences of lambda
calculus — this lecture's rules only need α and β, but η is worth recognizing
when you meet it elsewhere.

</ConceptSection>

<ConceptSection id="evaluation-rule-meaning" title="What an evaluation rule means">

Typing rules ask "does this make sense"; evaluation rules ask "what happens
next". Each one is a **rewrite**: this exact shape becomes that shape, in one
step — like a single line of arithmetic, `2 + 2 → 4`. The arrow always means
"reduces to in one step", never "equals".

One more word shows up in the rules below: a **value** is just a term that's
already finished — nothing left to reduce. `true`, `5`, and any unapplied
function like `λx:Bool. x` are values; `3 + 1` is not — it can still be reduced, to `4`.

Two more rules round things out: reduce the function position first if it isn't
a value yet, then — once it is — reduce the argument. Together, these three
rules fully determine how any application runs:

</ConceptSection>

<RuleCardStrip ruleIds={["E-AppAbs", "E-App1", "E-App2"]} />

<Callout title="The Evaluation panel" screenOnly>

Back in the app, the **Evaluation** panel has its own strategy dropdown, a
**Step**/**All** toggle to walk through one reduction at a time or see the whole
trace at once, and a **Γ context** switch that shows any global `let`/function
declarations in scope alongside each step.

</Callout>

<ConceptSection id="evaluation-strategies" title="Evaluation strategies: which redex first?">

A term can contain more than one reducible expression at once — a **redex**, short
for "reducible expression," is any piece of a term one of the rules above can fire
on right now. The rules don't say which redex goes first if there's more than one; a
**strategy** does — like a cook facing several pots that could all be stirred right
now, and needing a rule for which one to touch first. The app supports three:

- **Call by value** (the default) — reduce an argument to a value *before*
  substituting it in. What most real languages do.
- **Call by name** — substitute the argument in unevaluated, and only reduce it
  once/if it's actually used. Never reduces under a `λ`.
- **Normal order** — always reduce the leftmost, outermost redex first,
  including under a `λ`.

The choice is observable: if a parameter is used more than once in the body,
call-by-name substitutes the unevaluated argument at *every* use and re-reduces
it each time, while call-by-value reduces it once, up front. Try the term below
under each strategy and compare the step counts:

</ConceptSection>

<ReductionStrategyExplorer
  term={"(λ x : Nat . x + x) ((λ y : Nat . y + 1) 3);"}
  hint="x is used twice in the body — watch how many times the inner (λy:Nat.y+1) 3 gets reduced under each strategy."
/>

<ConceptSection id="non-termination" title="When evaluation never stops">

So far, every strategy just took a different *number* of steps to reach the same
answer. But not every term reaches an answer at all — some loop forever, the
same way two mirrors facing each other reflect an image back and forth with no
final frame. Take the untyped term `ω = λx. x x` — "a function that calls its
argument on itself" — and apply it to itself: `ω ω`. Substituting gives back
`ω ω` again. And again. Forever.

Try to give `x x` a type in this lecture's language, though, and you get stuck:
`x` would need to be some type `T` *and* a function `T -> T2` at the same time —
plain STLC has no such type, so it never type-checks. That's not a coincidence —
it's a real theorem, **strong normalization**: every well-typed STLC term is
guaranteed to finish, under *every* strategy. (This is a stronger guarantee than
merely *weak* normalization, "some strategy finishes eventually" — STLC promises
all of them do.) That guarantee is also a direct descendant of the
Entscheidungsproblem story from the start of this lecture: `ω ω` is a small, tame
example of the same kind of non-terminating computation Turing and Church proved
can't always be predicted from the outside. (Lecture 7 reopens the door to loops
on purpose, with a new construct, `fix` — exactly because plain STLC can't do it
by itself.)

Termination can even depend on the strategy: `(λx. y) ω ω` — a function that
ignores its argument and always returns `y`. Under call by name, `y` comes back
immediately, since the argument is never touched. Under call by value, the
argument is reduced *first*, before the function ever runs — and since `ω ω`
never finishes, neither does this.

</ConceptSection>

<ConceptSection id="church-rosser" title="Does the order ever change the answer?">

Strategies can disagree on speed, and — as above — on whether they finish at
all. But when two of them *do* both finish, they can never disagree on the
result — the same way a maze can have several different paths to the exit, but
every path that actually reaches an exit leads to the *same* exit. That
guarantee is the **Church–Rosser theorem**: however many different valid paths
a term has, any two that reach a value reach the *same* value. Pick any legal
order of reductions you like — the answer, if there is one, was never in doubt.

</ConceptSection>

<SummaryBox
  id="summary"
  points={[
    {label: "Type systems", detail: "TT is static, strong, and explicit — checked before running, never silently converted, fully annotated by hand (for now)."},
    {label: "Syntax", detail: "a term is a variable, an abstraction, an application, or a constant — abstract syntax trees, free/bound names, α-equivalence, and η as a quieter third equivalence."},
    {label: "Typing", detail: "Γ ⊢ t : T asks whether a term makes sense; five rules cover every shape this lecture's grammar allows."},
    {label: "Semantics", detail: "β-reduction runs a term step by step; the strategy picks the redex order, but Church–Rosser guarantees the answer never changes, and strong normalization guarantees it always finishes."},
  ]}
/>

<ReferenceList
  id="sources"
  entries={[
    {
      label: "TUKE Type Theory course — Lecture 1 (Introduction, NBL, Lambda Calculus)",
      href: "https://kurzy.kpi.fei.tuke.sk/tt/lectures/01.html#:~:text=%2D-,kalkul,-Je%20univerz%C3%A1lny%20form%C3%A1lny,",
    },
    {
      label: "Pierce, B. C. — Types and Programming Languages (MIT Press, 2002)",
      href: "https://www.cis.upenn.edu/~bcpierce/tapl/",
    },
  ]}
/>
